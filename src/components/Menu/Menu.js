import React from 'react';
import PropTypes from 'prop-types';
import './Menu.scss';

const Menu = () => (
  <div className="Menu">
    Home
  </div>
);

Menu.propTypes = {};

Menu.defaultProps = {};

export default Menu;
